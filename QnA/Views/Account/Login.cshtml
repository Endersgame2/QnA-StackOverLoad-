@model QnA.Models.Account.LoginViewModel
<h1>
    Login
</h1>
@Html.Partial("_Notification", Model.Notification)
<div class="row">
    <div class="col-sm-12">
        <form asp-controller="Account" asp-action="Login" method="post">
            <div class="form-row">
                <div class="form-group col-sm-12">
                    @Html.LabelFor(x => x.Key)
                    @Html.TextBoxFor(x => x.Key, new {@class = "form-control", @required="true"})
                    @Html.ValidationMessageFor(x => x.Key)
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-sm-12">
                    @Html.LabelFor(x => x.Password)
                    @Html.PasswordFor(x => x.Password, new {@class = "form-control", @required="true"})
                    @Html.ValidationMessageFor(x => x.Password)
                </div>
            </div>
            <div class="form-row mt-4">
                <div class="form-group col-sm-12">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </div>
        </form>
    </div>
</div>
